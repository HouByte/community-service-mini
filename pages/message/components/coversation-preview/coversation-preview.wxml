<view class="container">
    <view class="left">
        <image class="avatar-image" src="{{user.avatar}}"></image>
    </view>
    <view class="right">
        <view class="right-top">
            <view class="nickname">{{user.nick}}</view>
            <view class="last-send-time">{{_lastMessage.lastTime}}</view>
        </view>
        <view class="right-bottom">
            {{_lastMessage.messageForShow}}
        </view>
    </view>
</view>