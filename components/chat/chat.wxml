<view class="container">
    <i-avatar src="{{userInfo.avatarUrl}}" text="{{userInfo.nickName}}" shape="circle" size="60" ></i-avatar>
    <view bind:tap="handToChat">
        <i-button special icon="telephone">联系对方</i-button>
    </view>
</view>